services:
  hue-exporter:
    image: ghcr.io/hypercat-net/hue-exporter:latest
    container_name: hue-exporter
    command:
      - --config.file=/config/hue_exporter.yml
      - --storage.file=/data/hue_exporter_state.yml
      - --web.listen-address=:9366
    volumes:
      # User-managed config file; the exporter does not rewrite it.
      - ./hue_exporter.yml:/config/hue_exporter.yml:ro
      # Exporter-managed state (generated app_key + discovered bridge_ip).
      - hue-exporter:/data
    ports:
      - "9366:9366"
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - /hue-exporter
        - --healthcheck.target=http://127.0.0.1:9366/healthz
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

volumes:
  hue-exporter:
